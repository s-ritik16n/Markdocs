export const copy = (event, text) => {
  console.log(text);
}

export const cut = (event, text) => {
  console.log(text);
}

export const paste = (event, text) => {
  console.log(text);
}

export const headers = (event, text) => {
  console.log(text);
}

export const link = (event, text) => {
  console.log(text);
}

export const image = (event, text) => {
  console.log(text);
}

export const lists = (event, text) => {
  console.log(text);
}

export const table = (event, text) => {
  console.log(text);
}

export const lineBreak = (event, text) => {
  console.log(text);
}

export const codeBlock = (event, text) => {
  console.log(text);
}

export const code = (event, text) => {
  console.log(text);
}

export const blockQuote = (event, text) => {
  console.log(text);
}

export const clearText = (event, text) => {
  console.log(text);
}
