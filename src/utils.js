const copy = (event, text) => {
  console.log(text);
}

const cut = (event, text) => {
  console.log(text);
}

const paste = (event, text) => {
  console.log(text);
}

const headers = (event, text) => {
  console.log(text);
}

const link = (event, text) => {
  console.log(text);
}

const image = (event, text) => {
  console.log(text);
}

const lists = (event, text) => {
  console.log(text);
}

const table = (event, text) => {
  console.log(text);
}

const lineBreak = (event, text) => {
  console.log(text);
}

const codeBlock = (event, text) => {
  console.log(text);
}

const code = (event, text) => {
  console.log(text);
}

const blockQuote = (event, text) => {
  console.log(text);
}

const clearText = (event, text) => {
  console.log(text);
}

export * from  ...;
